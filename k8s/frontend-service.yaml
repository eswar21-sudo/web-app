apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80    # Expose port 80
      targetPort: 80  # The port where the frontend container listens
  type: LoadBalancer  # Expose the service externally via a cloud provider's load balancer
