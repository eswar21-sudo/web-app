apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000   # Expose port 5000 for the backend
      targetPort: 5000  # The port where the backend container listens
  type: ClusterIP  # This will expose the backend internally in the cluster
